<h1>Review  for <%= @restaurant.name %></h1>
<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating %>
  <%= f.submit "add your review", class: "btn btn-primary" %>
  <%= link_to "cancel", restaurant_reviews_path, class: "btn btn-danger" %>
<% end %>
